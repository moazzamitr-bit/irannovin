{
  "name": "@garm/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prisma:validate": "DATABASE_URL=\"${DATABASE_URL:-postgresql://garm:garm@localhost:5432/garm?schema=public}\" prisma validate",
    "prisma:format": "prisma format",
    "prisma:generate": "prisma generate",
    "typecheck": "DATABASE_URL=\"${DATABASE_URL:-postgresql://garm:garm@localhost:5432/garm?schema=public}\" prisma validate"
  },
  "dependencies": {
    "@garm/domain": "*",
    "@garm/financial": "*",
    "@garm/providers": "*"
  },
  "devDependencies": {
    "prisma": "^6.19.0"
  }
}
